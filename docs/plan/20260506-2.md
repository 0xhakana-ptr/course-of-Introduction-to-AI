# 2026-05-06 AI 桌宠后端后续完善计划

## 1. 项目目标判断

当前项目不是普通 Web 后台，而是一个面向 **Live2D AI 桌宠** 的本地后端。

因此后端的核心目标不是堆叠复杂企业级能力，而是稳定支撑以下几件事：

1. 让聊天窗口可以接入真实 AI。
2. 让 AI 回复具备稳定的人格和上下文。
3. 让 AI 能通过消息协议驱动桌宠的字幕、表情、动作和状态。
4. 让 LangGraph 作为 Agent Brain 组织状态流、节点和工具调用。
5. 让代码任务或工具任务可以安全执行，并把执行过程反馈给前端。
6. 保持本地桌面项目的轻量、可调试和可协作。

一句话概括：

> 后端应该成为桌宠的“大脑”和“行为调度层”，而不是泛化成一个臃肿的通用平台。

这里需要明确一个核心边界：

1. `LongCat` 是大模型本身，是 LLM provider，负责推理、生成、判断和写作。
2. `LangGraph` 是 Agent Brain 的编排框架，负责状态流转、条件分支、节点协作和工具调用。
3. `safe_fs`、`safe_execute_command`、`/runs`、`character_interface` 是工具和能力层。
4. 真正的 Agent 不是单独的 LongCat，而是 `LangGraph + LLM + Tools + State + Safety` 的组合。

## 2. 当前后端状态

当前后端已经具备较完整的基础骨架：

1. `FastAPI` 主入口可以正常启动。
2. `/chat` 已经可以处理普通聊天、测试命令和实验性 coding 分支。
3. `/runs` 已经支持任务创建、执行、自动修复、重试、重新运行、取消、日志查询和 attempts 查询。
4. `/messages` 已经支持统一消息队列和前端轮询。
5. `/llm/diagnostics` 已经支持本地配置检查和远程连通性诊断。
6. `safe_fs` 和 `safe_execute_command` 已经提供 workspace 边界和危险命令拦截。
7. 后端自动化测试已经覆盖启动、聊天、LLM、消息队列、runs 生命周期和 safe tools。
8. `agent_workflow` 已升级为最小 LangGraph Agent Brain，当前 coding intent 已能通过 Run Tool Node 创建 run；完整 QA、Repair 和多 Agent 协同仍后置。

当前后端已经不是“能不能跑”的阶段，而是进入了“是否贴合桌宠产品目标”的阶段。

## 3. 当前还缺什么

### 3.1 缺 AI 回复到角色表现的稳定链路

现在后端可以返回聊天文本，也可以通过消息队列发送 `quip`、`expression`、`chat`、`status`、`error`。

但还缺一个统一策略来决定：

1. AI 什么时候只回复聊天文本。
2. AI 什么时候发字幕窗口的打趣话语。
3. AI 什么时候切换表情。
4. AI 什么时候播放动作。
5. AI 什么时候发送任务进度状态。

这是 AI 桌宠项目最关键的缺口。用户感受到的不是“后端有接口”，而是“角色真的在思考、回应和表现情绪”。

### 3.2 缺统一的角色人格与系统提示词

当前 `.env.example` 中有 `LLM_SYSTEM_PROMPT`，但它仍然偏通用助手。

后续需要明确：

1. 桌宠的人格设定。
2. 回复语气。
3. 专业回答和轻松吐槽的边界。
4. 长回答和短回答的策略。
5. 是否允许主动追问。
6. 是否允许通过消息队列触发表情或动作。

这部分会直接决定项目体验，比继续增加后端接口更重要。

### 3.3 缺聊天上下文管理

当前 `ChatRequest` 虽然有 `context` 字段，但后端自身没有维护会话历史。

这意味着：

1. 如果前端不传完整上下文，后端就不会记得前面说过什么。
2. 多轮聊天体验会偏割裂。
3. 后续想做“桌宠记忆”会缺基础结构。

建议先做轻量版本：

1. `session_id`
2. 最近 N 条消息
3. 清空会话
4. 可选摘要

### 3.4 缺后端到 Live2D 行为的正式适配层

前端已经提供 Live2D WebSocket 控制接口，并支持表情标签映射，例如 `tag:开心`。

后端现在还没有一个正式服务来表达：

1. `thinking` 应该对应什么字幕和表情。
2. `done` 应该对应什么表情和动作。
3. `error` 应该对应什么表情和状态。
4. `coding` 应该对应什么状态提示。

建议新增一个角色事件服务，例如：

```text
backend/app/services/character_action/
backend/app/services/character_interface.py
```

它不需要直接绑定具体 Live2D 文件名，而是优先输出通用语义，例如：

```text
tag:开心
tag:疑惑
tag:思考
tag:难过
```

### 3.5 需要继续完善 coding 分支和 `/runs` 的统一边界

当前 `/chat` 的 `coding` 分支已经进入最小 LangGraph Agent Brain，并由 Run Tool Node 复用现有 run service 创建任务。

后续还需要继续补齐：

1. 将普通 chat intent 也逐步纳入 LangGraph，同时不破坏现有会话记忆。
2. 让 LangGraph 能读取 run 执行结果，而不是只负责创建 run。
3. 增加 QA / Repair Node，用于分析失败日志并决定是否触发修复。
4. 让 Roleplay Node 更稳定地根据 run 状态发送字幕、表情和状态。
5. 保持 `/messages` 持续发送任务状态、表情和最终结果。

这样可以继续避免 `coding` 分支和 `/runs` 各做一套任务执行，同时也符合“LangGraph 才是 Agent Brain，大模型只是脑子”的团队方向。

### 3.6 缺更完整的 LangGraph Agent Brain

当前后端已经有最小 LangGraph 图，但还没有形成完整的“规划、执行、读取结果、分析、修复、总结”闭环。

下一步需要补或继续细化的是：

1. Intent Router Node：继续提高意图分类准确率，但不保留复杂历史。
2. Chat Node：逐步承接普通聊天，同时复用现有 LLM client 和 conversation store。
3. Roleplay Node：只负责桌宠人格、字幕、表情和状态，不读取后台工程噪声。
4. Coding Node：把用户需求转成更结构化的可执行任务。
5. Run Tool Node：从“创建 run”升级为“创建、读取、总结 run”。
6. QA / Repair Node：读取执行结果、错误和日志，再决定是否修复。
7. End Node：汇总结果并通知前端。

LangGraph 的价值不在于替代 LongCat，而在于把这些节点和状态组织起来，让大模型在正确的位置发挥作用。

### 3.7 缺更清晰的前后端消息协议最终版

当前 `MESSAGE_TYPE` 有：

1. `quip`
2. `expression`
3. `chat`
4. `error`
5. `status`

但项目本身还支持 Live2D motion，因此后续需要决定：

1. 是否新增 `motion`。
2. 是否把 `expression` 和 `motion` 合并为 `live2d_action`。
3. `expression` 字段到底传通用标签还是真实 action id。
4. `quip` 和 `chat` 的使用边界。
5. `status.progress` 前端是否展示。

这个协议应尽早写进文档，方便前端同学稳定接入。

## 4. 当前不太需要什么

### 4.1 暂时不需要完整多 Agent 公司化协同

当前 `agent_workflow` 已经从线性演示流程收敛为最小 LangGraph 图，但还没有真正形成完整的规划、执行、分析闭环。

建议：

1. 保留 LangGraph 作为主线方向。
2. 暂时不急着做 PM、Architect、Coder、QA 全量多 Agent 公司。
3. 先做一个最小可用 LangGraph：`router -> chat/coding -> roleplay -> done`。
4. 等最小图稳定后，再扩展 PM、Coder、QA、Repair 等节点。

也就是说：不是不要 LangGraph，而是先做小图，不要一上来就做完整多 Agent 公司。

### 4.2 暂时不需要用户系统和数据库

这是本地桌面项目，目前没有多人协作、账号登录、远程权限隔离需求。

短期不建议加入：

1. 用户注册登录。
2. 权限系统。
3. 复杂数据库。
4. ORM 层。

当前文件存储 runs 结果已经足够支撑开发和课程展示。

### 4.3 暂时不需要多厂商 SDK

当前 OpenAI-compatible 主模型和 fallback 模型已经足够，LongCat 可以作为当前主要大模型。

不建议现在引入大量厂商专用 SDK。只要 provider 支持 OpenAI-compatible `/chat/completions`，就继续走同一套 LLM client。

### 4.4 暂时不需要把 `/runs` 全部功能一次性接给前端

`retry`、`rerun`、`cancel`、`attempts`、`logs` 对后端调试很有价值，但前端第一阶段不一定都要展示。

前端最小接入可以先只使用：

1. 创建任务。
2. 查询状态。
3. 展示最终结果。
4. 接收状态消息。

### 4.5 暂时不需要保留正式主线外的 mock 入口

`backend/app/mock_backend.py` 更像早期调试工具，和正式 `/messages` 链路有重复。

建议后续二选一：

1. 如果仍有调试价值，移动到 `backend/devtools/` 或 `scripts/`。
2. 如果前端已经不依赖它，删除或归档。

## 5. 优化方向

### 5.1 路由拆分

当前 `main.py` 同时承担多组接口路由，后续建议拆分为：

1. `backend/app/api/health_routes.py`
2. `backend/app/api/llm_routes.py`
3. `backend/app/api/chat_routes.py`
4. `backend/app/api/run_routes.py`
5. `backend/app/api/message_routes.py`

拆分后 `main.py` 只负责：

1. 创建 FastAPI app。
2. 注册 lifespan。
3. 注册 routers。
4. 配置 logging。

### 5.2 统一角色事件服务

建议新增角色事件层，封装桌宠表现逻辑。

示例能力：

1. `send_thinking()`
2. `send_chatting()`
3. `send_task_started()`
4. `send_task_done()`
5. `send_task_failed()`
6. `send_cancelled()`

这样 `/chat`、`/runs` 和未来 Agent 工作流都可以复用同一套表现策略。

### 5.3 聊天记忆

建议新增轻量会话存储：

```text
backend/app/storage/conversation_store.py
```

第一版不需要复杂，只需要：

1. 创建或复用 `session_id`。
2. 保存最近若干轮对话。
3. 限制最大长度。
4. 支持清空会话。
5. 可选持久化到 workspace。

### 5.4 coding intent 接入 `/runs`

后续应逐步把 `chat_action/coding.py` 从直接桥接实验性 `agent_workflow`，调整为进入 LangGraph Agent Brain。

建议行为：

1. `/chat` 调用 LangGraph 入口。
2. Intent Router 判断 `coding`。
3. Coding Node 准备任务输入。
4. Run Tool Node 调用现有 run service 创建 run。
5. 后台执行 run。
6. Roleplay Node 根据 run 状态发送角色事件。
7. End Node 返回 `run_id` 和简短说明。

### 5.5 LangGraph Agent Brain 最小化改造

建议把 `agent_workflow` 升级为正式的 Agent Brain。

第一版只需要最小图：

```text
router -> chat_node -> roleplay_node -> done
router -> coding_node -> run_tool_node -> roleplay_node -> done
router -> unknown_node -> roleplay_node -> done
```

第一版状态建议：

```text
user_input
session_id
intent
chat_output
run_id
ui_status
error
```

状态设计原则：

1. 不把所有历史都塞进一个全局 `messages`。
2. 聊天上下文走 `conversation_store`。
3. 代码任务上下文走 `/runs` 和 artifacts。
4. 前台角色表现只读 `intent`、`ui_status`、`run_id`、`error` 等摘要信息。
5. 后台工程错误不要污染 Roleplay Agent 的长上下文。

### 5.6 整理实验模块

建议明确区分：

1. 正式主线：`/chat`、`/runs`、`/messages`、LLM client、safe tools。
2. Agent 主线：`agent_workflow`，逐步升级为 LangGraph Agent Brain。
3. 调试工具：`mock_backend.py`。

这样团队协作时不容易把实验代码误认为正式入口。

### 5.7 schema 和内部状态继续收敛

建议遵循：

1. 机器读的字段用稳定英文枚举。
2. 用户可见的 `output`、`summary`、`content` 用中文。
3. 文档中明确每个消息字段的含义。
4. 不让前端依赖临时字符串判断状态。

## 6. 分阶段计划

### P0：协议收口

目标：先把桌宠表现协议定下来。

任务：

1. 整理 `/messages` 的正式消息协议。
2. 判断是否新增 `motion` 或 `live2d_action`。
3. 明确 `quip`、`chat`、`expression`、`status` 的边界。
4. 更新 `docs/backend-api-specification.md`。
5. 补充消息 schema 测试。

产出：

1. 前后端都能依据同一份消息协议联调。
2. 后端不再随意拼临时消息结构。

### P1：角色事件服务

目标：让后端可以稳定驱动桌宠表现。

任务：

1. 新增 `character_interface.py`。
2. 新增 `character_action/`。
3. 封装常见事件：思考、回复、任务开始、任务完成、任务失败、取消。
4. 将 `/chat` 和 `/runs` 中的状态反馈逐步接入角色事件服务。

产出：

1. 角色表现逻辑从业务流程中抽离。
2. 前端能更稳定地收到字幕、表情和状态。

### P2：聊天人格和上下文

目标：让桌宠更像持续存在的角色，而不是一次性问答接口。

任务：

1. 优化 `LLM_SYSTEM_PROMPT` 默认模板。
2. 新增会话存储。
3. 扩展 `ChatRequest` 支持 `session_id`。
4. 扩展 `ChatResponse` 返回 `session_id`。
5. 增加清空会话接口。
6. 补充上下文测试。

产出：

1. 多轮对话更连续。
2. 角色风格更稳定。

### P3：coding intent 统一到 `/runs`

目标：通过 LangGraph 统一 coding intent、runs 链路和角色事件，避免聊天入口、实验 Agent 和 runs 各走各的。

任务：

1. 重构 `agent_workflow` 为最小 LangGraph Agent Brain。
2. 新增 Intent Router Node。
3. 新增 Chat Node，复用现有 LLM client 和 conversation store。
4. 新增 Coding Node，负责把用户请求转成 run 输入。
5. 新增 Run Tool Node，复用现有 run service。
6. 新增 Roleplay Node，复用 `character_interface`。
7. 调整 `chat_action/coding.py`，让 coding intent 进入 LangGraph，而不是直接走独立逻辑。

产出：

1. LongCat 作为 LLM provider 被 LangGraph 节点调用。
2. LangGraph 成为真正的 Agent Brain。
3. 代码任务统一走成熟的 runs 生命周期。
4. 前端可以通过同一套消息机制展示任务进度。

### P4：路由拆分与工程整理

目标：降低入口复杂度，提高后续维护性。

任务：

1. 新增 `backend/app/api/`。
2. 拆分 health、llm、chat、runs、messages routes。
3. 保持 `main.py` 只负责 app 编排。
4. 整理 `mock_backend.py` 的位置或用途。
5. 更新 README。

产出：

1. 后端入口更清晰。
2. 团队成员更容易定位接口实现。

### P5：更细工具能力

目标：为后续真实 Agent 能力打基础。

任务：

1. 增加读取项目文件工具。
2. 增加列目录工具。
3. 增加运行测试工具。
4. 增加错误摘要工具。
5. 所有工具继续受 workspace 安全边界约束。

产出：

1. 后续 Agent 可以更安全地处理真实项目任务。
2. 不需要让 LLM 直接拼危险命令。

### P6：Workspace Tool Node 与安全工具编排

目标：把已经存在的安全工具正式接入 LangGraph，使 coding 分支在创建 run 前先具备受控的项目感知能力。

任务：

1. 将读文件、列目录、运行测试、错误摘要等工具整理为稳定的 registry。
2. 为工具规划结果定义统一的 `tool_name + tool_input + reason` 结构。
3. 在 LangGraph 中形成 `coding_node -> workspace_tool_node -> run_tool_node` 的显式链路。
4. 约束工具失败时的降级策略，避免工具异常直接阻断主链路。
5. 补充工具节点、工具计划和安全边界测试。

产出：

1. coding 分支在创建 run 前可以自动补充安全、精简、可解释的 workspace 上下文。
2. 后续继续增加 Agent tools 时，不需要反复堆叠新的分支判断。

### P7：run 读取与控制闭环

目标：让 LangGraph 不只会“创建 run”，还可以读取已有 run，并通过 `/chat` 发起受控的 run 生命周期操作。

任务：

1. 新增统一的 run snapshot 结构和读取接口。
2. 让 `/chat` 能识别已有 `run_id` 的状态查看请求。
3. 让 `/chat` 能识别已有 `run_id` 的 `retry / rerun / cancel` 控制请求。
4. 明确 `create / retry / rerun` 与 `inspect / cancel` 的后台调度边界。
5. 继续补 run 中间态反馈，让 LangGraph 能决定继续等待、发送阶段性说明或进入总结。

产出：

1. `/chat` 不再只适合创建新任务，也可以管理已有代码任务。
2. LangGraph 开始具备“创建 -> 读取 -> 控制 -> 总结”的最小闭环能力。

### P8：Agent Workflow 诊断与可观测性

目标：降低 LangGraph、LLM、tool 和 run 链路联动时的排错成本。

任务：

1. 为主要 workflow 节点补充更清晰的 trace、node log 和状态快照。
2. 增加面向开发期的 diagnostics/debug 入口，便于定位节点路由与失败原因。
3. 统一 graph、tool、LLM、roleplay 相关错误的上下文输出方式。
4. 为关键 workflow 行为补充更容易定位问题的测试和文档。

产出：

1. 后续扩展 LangGraph 节点时更容易判断问题出在路由、工具、模型还是运行链路。
2. 团队协作时对 workflow 内部状态的观察成本更低。

### P9：Workflow 边界收敛与持续去冗余

目标：在功能继续增加的同时，避免 `agent_graph.py`、support/helper 和结果模型再次膨胀。

任务：

1. 继续把状态写回、结果归一化和共享文案下沉到明确的 helper 边界。
2. 收敛 chat / run / summary / repair / tool 几条链路中的重复状态字段和控制流。
3. 保持 graph 文件主要承担“节点定义 + 编排”，避免重新堆回业务细节。
4. 为去冗余后的关键边界补回归测试，避免重构带来行为漂移。

产出：

1. workflow 模块结构更清晰，后续扩展多 Agent 或更多节点时不容易失控。
2. 代码维护成本和理解成本都更低。

## 7. 推荐执行顺序

建议按以下顺序推进：

1. P0：消息协议收口。
2. P1：角色事件服务。
3. P2：聊天人格和上下文。
4. P3：coding intent 接入 `/runs`。
5. P4：路由拆分和 Agent 模块整理。
6. P5：工具能力扩展。
7. P6：Workspace Tool Node 与安全工具编排。
8. P7：run 读取与控制闭环。
9. P8：Agent Workflow 诊断与可观测性。
10. P9：Workflow 边界收敛与持续去冗余。

其中，最优先的是 P0 和 P1。

原因是它们最贴合 AI 桌宠项目目的，也最方便前端同学尽快联调。

## 8. 当前阶段的判断

当前后端已经具备可运行、可测试、可继续扩展的基础。

下一步不应该优先追求复杂多 Agent，而应该先完成：

1. 桌宠消息协议。
2. 角色表现策略。
3. 聊天上下文。
4. LangGraph Agent Brain 与 `/runs` 的统一。

这样项目会从“有 AI 接口的前端应用”，推进到“真的像 AI 桌宠的本地应用”。

## 9. 当前执行进度

截至 2026-05-07，已完成：

1. P0：消息协议收口。
   已将 `motion` 纳入正式后端消息协议，并更新 `MessageEnvelope`、`MessageSender`、协议文档和测试。

2. P1：角色事件服务基础版。
   已新增 `character_interface.py` 和 `character_action/`，统一封装聊天和任务运行中的桌宠表现事件。

3. P2：聊天人格和上下文基础版。
   已优化默认 `LLM_SYSTEM_PROMPT`，新增轻量 `conversation_store.py`，并为 `/chat` 增加 `session_id` 输入输出和会话清空接口。

4. P3：LangGraph coding intent 基础版。
   已将 `agent_workflow` 从线性演示流程收敛为最小 LangGraph 图，包含 `router`、`chat_node`、`coding_node`、`run_tool_node`、`unknown_node` 和 `roleplay_node`。
   当前 `/chat` 的 coding intent 会进入 LangGraph，由 `run_tool_node` 复用现有 run service 创建 run，并返回 `run_id`。

5. P3：LangGraph 普通聊天接入版。
   已将普通 `chat intent` 也纳入最小 LangGraph 图，当前 `/chat` 的普通聊天和 coding 都统一经过 `agent_workflow`。
   会话记忆、LLM client、聊天生命周期事件仍保持现有行为，同时避免把同一条聊天正文通过 HTTP 响应和 `agent:chat` 队列重复输出。

6. P3：`/runs` 最终结果消息闭环版。
   已在 run 成功、失败、取消和异常结束时，统一生成简洁的任务总结文本并写入 `agent:chat`。
   当前 coding intent 在 `/chat` 返回 `run_id` 后，前端还可以继续通过 `/messages` 收到最终任务结果，而不会重复收到初始说明文本。

7. P3：LangGraph run 结果总结接入版。
   已新增独立的 `run_summary_graph`，用于读取终态 run record、生成用户可读总结，并统一发送最终 `agent:chat`。
   当前 LangGraph 已不再只负责“创建 run”，也开始承担“消费 run 结果并做结果总结”的职责；若总结节点不可用，则自动回退到确定性摘要文本。

8. P3：LangGraph repair decision 接入版。
   已新增独立的 `repair_decision_graph`，用于在单次执行失败后分析失败摘要、判断是否允许进入自动修复，并向现有 `/runs` repair 流程回传结构化决策。
   当前自动修复已经不再只是 service 层里的裸条件判断，而是先经过 LangGraph 的失败分析和 repair decision，再决定是否继续生成修复脚本。

9. P3：LangGraph repair generation 接入版。
   已将 repair graph 从“只做决策”继续扩展到“决策 + 生成修复脚本”。
   当前执行失败后，LangGraph 会先分析失败、判断是否修复，再在允许时直接生成修复脚本并回传给 `/runs` 主执行链路；service 层只保留状态写回、重试执行和最终收口。

10. P3：attempt 级 repair feedback 接入版。
   已将 repair workflow 的中间反馈文本并入 `repair_decision_graph` 输出。
   当前在进入自动修复和重试前，后端会先向 `agent:chat` 发送一条 attempt 级进展说明，包含本次失败摘要、分析结论和下一步动作；这样 repair 过程不再只体现在日志和状态栏中。

11. P3：retry outcome summary 接入版。
   已新增独立的 `attempt_summary_graph`，用于在自动修复后的重试尝试结束后，整理这一轮 attempt 的结果和下一步动作，并写入 `agent:chat`。
   当前 repair 成功路径会形成“进入修复前反馈 -> 重试结果总结 -> 最终 run 总结”的完整闭环；repair 后仍失败时，也会先发出本轮重试结果说明，再进入最终失败收口。

12. P2：会话记忆 workspace 持久化版。
   已将 `conversation_store` 从纯内存缓存升级为“内存缓存 + workspace 持久化”模式。
   当前聊天历史会写入 `backend/workspace/conversations/`，服务重启后仍可基于同一个 `session_id` 继续读取最近对话，同时保留现有 `session_id`、上下文拼接和清空接口行为。

13. P4：主入口路由拆分基础版。
   已新增 `backend/app/api/`，并将 `health`、`llm`、`chat`、`runs`、`messages` 五组路由从 `main.py` 中拆分出去。
   当前 `main.py` 已收敛为 `create_app()`、`lifespan` 和 router 注册入口，后续继续扩展接口时不需要再把所有实现堆回主文件。

14. P4：统一异常处理与 run 路由依赖收敛版。
   已新增 app 级异常处理注册，将 `RunActionError` 从各个路由内部的局部 `try/except` 收敛为统一 handler。
   同时已新增 `run_dependencies.py`，把 run、attempt、script、output、log 的读取与 404 判定收口为依赖层，降低 `run_routes.py` 的重复判断和分散感。

15. P2：持久化会话清理策略基础版。
   已为 `conversation_store` 增加基于 TTL 和最大文件数的自动清理机制，并支持启动时和运行期按间隔清理。
   当前 workspace 中的会话文件不再只增不减，而是会按 `CONVERSATION_SESSION_TTL_SECONDS`、`CONVERSATION_MAX_PERSISTED_SESSIONS` 和 `CONVERSATION_CLEANUP_INTERVAL_SECONDS` 进行约束。

16. P3：workflow support 去冗余版。
   已将 `run_summary_graph` 和 `attempt_summary_graph` 中重复的摘要解析、初始 state、roleplay 收口和 graph 编排骨架下沉到 `summary_support.py`。
   同时已将 `repair_decision_graph` 中的分析摘要解析、初始 state 构造和结果转换进一步收敛，使 summary / repair 子图之间的边界更一致，后续继续扩节点时不需要重复维护同类样板逻辑。

17. P3：repair retry guidance 结构化版。
   已让 `repair_decision_graph` 在 repair workflow 结果中直接返回“下一步动作”和对应的 retry 消息节点信息，而不是继续由 `run_interface` 手写同类文案。
   当前 LangGraph 对 repair 失败后的中间状态描述更完整，service 层主要负责消费结构化结果并发送消息，后续继续扩展多轮 repair 策略时不需要再把同类提示词散落在执行循环中。

18. P3：shared retry guidance result model 版。
   已新增共享的 `RetryGuidance` 结果模型，用于统一承载 retry 中间态消息的 `node_name` 和 `next_action`。
   当前 `repair_decision_graph`、`run_interface` 和 retry outcome 发消息链路已经围绕同一个结果对象协作，不再依赖多组分散字符串字段，后续继续扩展多轮 repair 或更多中间状态时会更容易保持一致。

19. P3：shared retry guidance policy 版。
   已新增独立的 `retry_guidance.py`，用于集中维护 retry 中间态消息节点与说明文本的映射策略。
   当前 retry 成功、取消、继续修复、结束失败等中间态文案不再散落在 `run_interface` 和 graph 节点里，而是通过共享 policy 生成 guidance，对后续继续扩展多轮 repair 的连续反馈更友好。

20. P3：repair phase resolution 下沉版。
   已新增统一的 repair phase resolution 结果对象，并把 repair 阶段的 retry guidance fallback、repair feedback 发送、状态阻断和取消收口下沉到 lifecycle helper。
   当前 `run_interface.execute_run()` 不再手写这段 repair 中间态胶水逻辑，主循环只负责执行 attempt、消费 repair phase 结果并决定继续、结束或取消，后续继续扩展多轮 repair 时更容易保持稳定。

21. P3：shared summary workflow result model 版。
   已新增共享的 `WorkflowSummaryResult`，并让 `run_summary_graph` 与 `attempt_summary_graph` 统一返回同一类结果对象。
   当前 summary 子图不再只依赖松散的字典结构，对外仍保留 `as_dict()` 兼容出口，内部则更容易继续向共享 workflow result 模型收敛。

22. P3：shared repair workflow result model 版。
   已新增共享的 `WorkflowRepairResult`，并让 `repair_decision_graph` 的“只做决策”和“决策 + 修复脚本 + feedback”两种出口统一返回同一类结果对象。
   当前 repair 子图也具备 `from_state()`、`as_dict()` 和原始 `state` 保留能力，`repair_decision_graph` 内部不再维护两套相近的结果转换胶水，summary / repair 两条链路的结果边界进一步对齐。

23. P4：shared API error response and router prefix 版。
   已新增统一的 `ApiErrorResponse` 与错误响应 helper，并让 `RunActionError`、`HTTPException`、`PermissionError` 和请求参数校验错误统一输出同一类 JSON 结构，同时保留 `detail` 兼容字段。
   此外已为 `chat`、`runs`、`messages`、`llm` 等 router 增加明确的 `prefix`、`tags` 和公共错误响应声明，`run_dependencies.py` 也已改为通过共享 helper 抛出标准化错误，P4 的接口层边界更清晰。

24. P2：conversation context compression 版。
   已为 `conversation_store` 增加“更早历史压缩摘要 + 最近消息原样保留”的上下文构建策略，并新增 `CONVERSATION_CONTEXT_RECENT_MESSAGES` 与 `CONVERSATION_SUMMARY_MAX_CHARS` 两个配置项。
   当前长会话不再只依赖末尾硬裁切，而是会优先保留最近若干条原始消息，同时把更早历史压缩成确定性摘要，降低上下文膨胀并尽量保住多轮连续性。

25. P2：persisted session metadata 版。
   已为会话持久化文件增加 `metadata`，并新增 `GET /chat/sessions/{session_id}` 用于读取当前会话的消息数量、压缩数量、摘要预览和更新时间等信息。
   当前会话层不再只有“消息列表 + 手动清空”，后端已经能直接观测长会话压缩是否生效，也为后续进一步做会话列表观测或调试面板打下基础。

26. P2：persisted session summary cache 版。
   已让 `conversation_store` 将长会话压缩得到的确定性摘要直接写入持久化 `metadata`，并在重载会话时优先复用兼容缓存，而不是每次重新压缩更早历史。
   当前摘要缓存会跟随 `context_strategy_version`、`CONVERSATION_CONTEXT_RECENT_MESSAGES`、`CONVERSATION_SUMMARY_MAX_CHARS` 和消息数量一起校验；配置变化或缓存失配时会自动重建，同时 `GET /chat/sessions/{session_id}` 也已暴露 `has_summary_cache` 与 `context_strategy_version` 便于调试。

27. P2：session list observability 版。
   已新增 `GET /chat/sessions` 分页接口，用于列出最近持久化的聊天会话及其元信息，包括消息数量、压缩状态、摘要缓存状态和更新时间。
   当前后端已经可以在不依赖前端改动的前提下，直接观察最近有哪些会话活跃、哪些会话已经触发压缩，以及哪些会话已生成可复用的 summary cache，便于后续继续调试 LangGraph 记忆链路或补后台管理面板。

28. P3：attempt terminal resolution 下沉版。
   已将 repair attempt 在成功、执行中取消、收到取消请求后的三类终态收口，进一步从 `run_interface.execute_run()` 下沉到 `lifecycle` helper。
   当前执行主循环不再手写这些 retry outcome 与 terminal finalizer 的分支拼接，而是统一通过 attempt terminal resolution 处理“是否补发 retry outcome、用哪个 node、随后进入哪种最终收口”，P3 的中间态与终态边界更集中。

29. P3：shared agent workflow result model 版。
   已新增统一的 `WorkflowAgentResult`，并让 `agent_graph.run_agent()` 从松散字典返回值收敛为结构化结果对象。
   当前 `agent_graph -> chat_action.agent -> chat_interface` 这条 LangGraph 主链路，已经和 summary / repair 子图一样拥有更稳定的结果边界；`build_agent_reply()` 不再依赖多组字符串 key 手动拆字典，后续继续扩展 `run_status`、`ui_status` 或更多 Agent 状态时更不容易出现跨层字段漂移。

30. P3：repair workflow result normalization 版。
   已让 `WorkflowRepairResult` 支持从正式 graph state、mapping 和 generic object 三类输入统一归一化，并补上 `feedback_node_name`、`retry_node_name`、`retry_next_action` 等派生读取能力。
   当前 `lifecycle.advance_repair_phase()` 已不再散落使用 `getattr` 兼容各种 repair workflow 形态，而是先统一转成 `WorkflowRepairResult` 再消费其结构化字段；这样无论是正式 LangGraph 输出，还是测试里的 fake workflow，对 service 层暴露的边界都更一致。

31. P3：shared graph prompt and invoke helper 版。
   已在 `summary_support.py` 中新增共享的 `build_prompt_text`、`apply_text_resolution`、`resolve_summary_node_state` 和 `run_summary_graph_workflow`，并让 `run_summary_graph`、`attempt_summary_graph`、`repair_decision_graph` 复用这些 helper。
   当前三条子图在 prompt 文本拼接、LLM 文本结果写回 state、summary graph invoke 收口这三类样板逻辑上更加统一；后续继续扩展 QA / Summary / Roleplay 节点时，不需要再在每个 graph 文件里重复维护近似的 prompt join 和 invoke 包装代码。

32. P3：repair graph mode and route helper 版。
   已新增独立的 `repair_support.py`，把 repair graph 的 workflow mode、decision 后路由、feedback 后路由、feedback message 组装，以及 graph invoke 收口整理为共享 helper。
   当前 `repair_decision_graph` 已不再自己维护 decision-only 和完整 repair workflow 这两种运行模式下的初始 state、next-step 路由和 invoke 样板；后续继续扩展 repair graph 节点时，可以围绕同一套 mode / route helper 演进，而不是在主 graph 文件里继续堆条件分支。

33. P3：agent graph support helper 版。
   已新增独立的 `agent_support.py`，把主 Agent 图中的意图路由选择、coding run 输出文案、unknown fallback 文案、初始 state 构造，以及 graph invoke 收口整理为共享 helper。
   当前 `agent_graph.py` 已更接近“只保留节点和图编排”的状态；主图和 repair 子图在 support/helper 抽取风格上也更一致，后续继续扩充 router、tool node 或更多 Agent 状态时，不需要再在主 graph 文件里堆积同类样板。

34. P3：agent node state helper 版。
   已继续在 `agent_support.py` 中新增主图节点级别的共享 helper，包括 state merge、chat result 写回、coding requested 写回、run create 成功/失败写回、unknown intent 写回，以及 roleplay 收口。
   当前 `agent_graph.py` 里的 `router_node`、`chat_node`、`coding_node`、`run_tool_node`、`unknown_node` 和 `roleplay_node` 已进一步退化为“节点内最小调用层”，主图节点逻辑和状态写回方式更一致，后续继续拆分 node helper 或扩展更多节点时不需要重复维护同类状态拼装逻辑。

35. P4：unexpected exception standardization 版。
   已为 app 级异常处理新增通用 `Exception` handler，并把 `500` 场景纳入共享 `ApiErrorResponse` 模型与公共错误响应声明。
   当前即使接口内部出现未预期异常，后端也会统一返回 `{ ok: false, error, detail }` 结构，而不是退回到默认的非统一 `500` 响应；这使接口层错误边界和测试覆盖进一步完整。

36. P4：route glue helper and query alias 版。
   已新增共享的 `query_params.py` 和 `route_support.py`，把接口层重复出现的查询参数定义、会话信息组装、LLM diagnostics 响应转换，以及后台 run 调度下沉为统一 helper。
   当前 `chat_routes.py`、`run_routes.py`、`llm_routes.py`、`message_routes.py` 和 `run_dependencies.py` 中的样板 glue 代码进一步减少，后续继续拆分 router 或新增接口时更容易保持参数边界与返回结构一致。

37. P3：workflow invoke normalization 版。
   已为 `agent`、`summary`、`repair` 三类 LangGraph 工作流新增统一的 invoke 收口：成功时统一走 `from_state`，异常时统一降级为结构化 `from_error` 结果，而不再让部分子图直接抛出异常、部分子图返回对象。
   同时已补齐 lifecycle fallback：当 `run_summary_graph` 或 `attempt_summary_graph` 返回失败结果时，运行链路会继续回退到确定性消息文本，不会因为“安全返回但不抛异常”而静默丢失桌宠消息。

38. P3：repair node state helper 版。
   已继续把 `repair_decision_graph.py` 中节点级别的状态写回逻辑下沉到 `repair_support.py`，新增 failure inspected、eligibility、decision、feedback composed 和 codegen result 等共享 helper。
   当前 repair 子图的 `inspect_failure_node`、`eligibility_node`、`decision_node`、`compose_feedback_node` 和 `repair_codegen_node` 已进一步退化为“节点内最小调用层”，和 `agent_graph` 的 helper 抽取风格更一致，后续继续扩展 QA / repair 中间态时不需要再重复维护同类 `{**state, ...}` 样板。

39. P3：summary fallback lifecycle helper 版。
   已把 `lifecycle.py` 中“调用 LangGraph summary workflow，失败则回退到确定性消息”的两段重复控制流收口为共享 helper，并补齐 workflow 直接抛异常场景下的 fallback 测试。
   当前 `emit_final_run_chat_message()` 与 `emit_retry_outcome_message()` 已围绕同一套 fallback 逻辑协作，后续继续扩展更多 summary / roleplay 中间态时，不需要再重复维护同类 try-except + result.ok 判断骨架。

40. P3：summary node builder helper 版。
   已继续把 `run_summary_graph.py` 和 `attempt_summary_graph.py` 中重复的 summary node / roleplay node 构造下沉到 `summary_support.py`，新增共享的 resolution node builder 和 roleplay node builder。
   当前两条 summary 子图已更接近“状态 schema + prompt builder + output builder + graph 编排”的配置式结构，和 `agent_support.py`、`repair_support.py` 的抽取方向进一步对齐，后续继续扩展新的总结类节点时不需要重复维护同类闭包和 roleplay 样板。

41. P3：workflow node policy constant 版。
   已新增共享的 `workflow_nodes.py`，把 `agent_roleplay`、`task_done`、`task_retry_failed` 等 workflow 节点名以及 run 终态节点选择逻辑收口为统一常量和 helper。
   当前 `events.py`、`retry_guidance.py`、`agent_support.py`、summary / repair helper、`lifecycle.py` 和 `run_interface.py` 已开始围绕同一套 workflow node policy 协作，后续继续扩展中间态消息或调整节点命名时，不需要再跨多个模块手动同步相同字符串。

42. P3：workflow chat text builder 版。
   已把 `repair feedback`、`retry outcome`、`run completion` 三类用户可见 workflow 文案的公共骨架下沉到共享的 `build_run_chat_text()` helper，并保持现有输出格式不变。
   当前 run 生命周期和 LangGraph 子图在“标题 + run_id + 关键字段 + 查看链接”这类消息结构上已经围绕同一套文本装配策略协作，后续继续扩展更多中间态说明时，不需要再重复维护同类多行字符串拼装逻辑。

43. P3：shared workflow text resolution node 版。
   已在 `summary_support.py` 中新增共享的 `build_state_text_resolution()` 与 `build_text_resolution_node()`，并让 `repair_decision_graph` 的 QA 分析节点复用同一套“LLM 文本解析 -> state 写回”骨架。
   当前 summary / repair 两类子图在文本字段生成、fallback 保底和 source 写回方式上进一步统一，后续继续扩展 analysis、roleplay 或其他轻量文本节点时，不需要再重复维护同类 resolution 样板逻辑。

44. P3：retry guidance lifecycle resolver 版。
   已把 retry 中间态里的“终态节点选择 + next_action 文案映射 + repair result 归一化 fallback”继续下沉到 `retry_guidance.py`，并让 `lifecycle.py` 直接复用共享 helper。
   当前 retry outcome 的消息决策策略不再分散在 `lifecycle` 中手写判断 `ok / cancelled / cancel_requested / should_attempt_repair`；后续继续调整 retry 节点命名或下一步话术时，可以围绕同一套 policy helper 演进。

45. P3：shared roleplay emit helper 版。
   已在 `roleplay.py` 中新增共享的 `emit_roleplay_message()` 与 `emit_roleplay_state()`，并让 `agent_support.py`、`summary_support.py`、`lifecycle.py` 复用同一套 workflow chat 发消息入口。
   当前 workflow 内部不再分别维护“state 发消息”“message 对象发消息”“fallback 文本发消息”三种近似实现；后续继续调整桌宠聊天消息出口时，可以围绕同一套 helper 统一演进。

46. P3：agent result to chat result boundary 版。
   已把 `WorkflowAgentResult -> ChatServiceResult` 的 intent / output / error / run_id 归一化入口收口到 `ChatServiceResult.from_agent_result()`，并为 `ChatServiceResult` 增加 `with_updates()`。
   当前 `chat_action/agent.py` 不再手写一遍 Agent 结果拆解，`chat_interface.py` 也不再原地修改 `result.output` / `result.session_id`；后续继续扩展 Agent 返回字段或调整 fallback 输出时，可以围绕同一套结果边界演进。

47. P3：repair result consumption helper 版。
   已为 `WorkflowRepairResult` 新增 `analysis_log_payload()`、`feedback_payload()` 和 `repaired_result_or()`，并让 `lifecycle.advance_repair_phase()` 直接复用这些 helper。
   当前 repair workflow 的消费层不再分散读取 `analysis_note / analysis_source / feedback_text / feedback_node_name / repaired_result` 这些字段；后续继续调整 repair 子图输出结构时，可以优先收口在 `WorkflowRepairResult` 上，而不是继续把兼容逻辑散在 lifecycle 中。

48. P3：summary result fallback emit helper 版。
   已为 `WorkflowSummaryResult` 新增 `from_value()`，并在 `summary_support.py` 中新增共享的 `emit_summary_workflow_with_fallback()`，让 run summary 与 attempt summary 的 fallback 发消息路径复用同一套结果归一化与发消息入口。
   当前 `emit_final_run_chat_message()` 与 `emit_retry_outcome_message()` 不再各自维护近似的“invoke workflow -> 判断 ok -> log -> fallback emit”控制流；后续继续调整 summary 子图返回结构或 fallback 策略时，可以优先围绕共享 summary result/helper 演进。

49. P3：agent result normalization symmetry 版。
   已为 `WorkflowAgentResult` 新增 `from_value()`，并让 `ChatServiceResult.from_agent_result()` 基于它统一消费 Agent 结果，而不是继续对任意对象直接做 `getattr()` 拆解。
   当前 `agent / summary / repair` 三类 workflow result 都已具备“从 object/mapping 统一归一化”的对称入口；后续继续调整 Agent 图的输出字段时，可以优先收口在 `WorkflowAgentResult` 上，而不是把兼容逻辑散在 chat service 层。

50. P3：chat result lifecycle helper 版。
   已继续为 `WorkflowAgentResult` 增加 `resolved_intent()`、`resolved_output()` 和 `run_payload()`，并让 `ChatServiceResult.from_agent_result()` 直接复用这些消费 helper。
   同时，`chat_interface.py` 中的 coding 调度结果改写、会话写回和 session 绑定已收口为独立小 helper；当前 chat service 不再分散维护三段近似的 intent 分支与结果回写逻辑，后续继续调整 Agent 输出字段或 coding 调度策略时，可以优先围绕结果模型和 chat lifecycle helper 演进。

51. P4：route response glue helper 版。
   已继续把接口层里重复的 prompt/context 清洗、会话清空响应、会话元信息响应，以及消息队列列表/清空响应下沉到 `route_support.py`。
   当前 `chat_routes.py`、`run_routes.py` 和 `message_routes.py` 更接近纯路由声明；后续继续拆分接口或调整统一响应结构时，不需要再在多个 router 中重复维护同类样板。

52. P5：workspace tools foundation 版。
   已新增 `workspace_tools.py`，提供面向 Agent 的安全目录枚举、文件文本预览、pytest 运行和命令失败摘要能力；同时把 `safe_fs.py` 扩展为可返回文件/目录混合列表的 `safe_list_entries()`。
   当前后端已经具备 `list directory / read file / run tests / summarize error` 这四类基础工具，并继续受 workspace 边界约束；后续如果要把它们正式挂到 LangGraph tool node，只需要继续做 workflow 接线，而不需要再回到底层补安全实现。

53. P3：coding workspace tool context 接线版。
   已继续把 `workspace_tools` 接到 LangGraph 的 coding 链路上：当前 `coding_node` 在进入 `run_tool_node` 之前，会自动为 coding 任务补充一份受控的 workspace 概览上下文，包含顶层目录结构以及常见项目文件的预览。
   当前 `workspace tools` 已不再只是孤立的底层函数，而是开始被 `agent_workflow` 真实消费；后续如果继续扩展更细的 file reader / test runner / error summarizer tool node，可以在这条已接线的 coding 上下文链路上继续演进。

54. P2：session context snapshot 版。
   已新增 `GET /chat/sessions/{session_id}/context`，用于直接查看当前会话在后端侧形成的上下文快照，包括压缩摘要、最近消息以及最终将送给 LLM 的 `context_text`。
   当前 `P2` 已不再只能观察摘要预览和计数元信息，而是可以直接验证“后端到底会把什么上下文拼给模型”；这让会话压缩、摘要缓存和 LangGraph 聊天记忆链路的调试边界进一步闭合。

55. 去冗余：conversation store internal context resolver 版。
   已把 `conversation_store.py` 中重复出现的消息切片、摘要缓存读取、context 文本拼接，以及“加载消息 + 解析 metadata”逻辑下沉到共享内部 helper。
   当前 `build_context()`、`get_session_metadata()` 和 `get_context_snapshot()` 不再各自重复维护同类上下文装配流程；同时 `context snapshot` 也不再通过再次调用 `build_context()` 触发重复计算。

56. 去冗余：workflow result normalization helper 版。
   已把 `WorkflowSummaryResult`、`WorkflowAgentResult` 和 `WorkflowRepairResult` 内部重复的 `state -> result` 构造流程、`ok` 解析、字段名集合，以及 `attr/state` 文本回退逻辑进一步收口到共享 helper。
   当前三类 workflow result 在 `from_state()` / `from_value()` 的内部路径上更一致，结果归一化边界更清晰；后续继续扩展 Agent / Summary / Repair 返回字段时，不需要再在每个结果类里重复维护近似的字段拆解和回退逻辑。

57. 去冗余：route support session response builder 版。
   已把 `route_support.py` 中 `session info / metadata / context / list` 这几类响应共享的字段装配逻辑继续收口到统一 payload/helper，并让 `chat_routes.py` 直接复用会话列表响应 builder。
   当前接口层的 session 响应不再在多个 builder 中重复维护同一组消息数量、摘要缓存和更新时间字段；`route_support.py` 也更接近“通用响应装配层”，后续继续调整会话响应结构时只需要改一处核心 payload 逻辑。

58. P6：LangGraph workspace tool node 接线版。
   已把 `coding` 分支从“直接拼接 workspace overview”调整为“工具计划 -> 工具执行 -> run 创建”的显式链路。
   当前 `agent_workflow` 会先根据 prompt 选择 `build_workspace_overview / read_workspace_text / list_workspace_entries / run_workspace_tests` 之一，再把工具结果并入 run 上下文后交给 `run_tool_node`；同时保留工具失败不阻断主链路的收口策略，并补齐了相关自动化测试。

59. 通信层：正式消息 WebSocket 入口版。
   已在保留 `GET /messages` 轮询接口的前提下，新增正式的 `GET ws://.../messages/ws` 流式消息入口，并支持基于 `since_id` 的初始增量快照。
   当前后端的消息总线不再只暴露轮询读取模式；前端或调试客户端既可以继续走现有 HTTP 轮询，也可以直接订阅 WebSocket 消息流，以更贴近架构文档中“桥接层以 WebSocket 为主”的方向。

60. P6：structured workspace tool plan + registry 版。
   已把 `workspace_tools.py` 中原本分散在 `plan_workspace_tool()` 与 `execute_workspace_tool_plan()` 里的松散字段和执行分支，收口为“统一 `tool_input` 结构 + 工具 registry + 工具定义解析”模式。
   当前 `agent_workflow` 持有的 `workspace_tool_plan` 已不再依赖顶层临时键值拼装；后续新增更多 workspace tools 时，可以优先注册到统一 registry，再接入 planner，而不需要继续在执行器里堆叠新的 `if/elif` 分支。

61. P7：run snapshot 结构化读取版。
   已新增 `GET /runs/{run_id}/snapshot`，并在 run 查询层补上统一的结构化任务快照模型，用于表达当前任务摘要、最近一次 attempt 摘要、任务是否仍在进行，以及推荐的下一步动作。
   当前 `agent_workflow` 在创建 run 后已经不再只返回 `run_id + status`，而是会额外读取一次 run snapshot，把“当前快照 + 下一步”并入 coding 分支的首轮输出；这使 LangGraph 开始具备“创建后读取任务状态”的最小能力边界。

62. P7：chat 侧 run inspection 分支版。
   已让 `agent_workflow` 能识别“查看已有 run 状态”的 coding 请求：当 prompt 中带有合法 `run_id` 且语义更接近状态查询时，LangGraph 会直接走 `run_snapshot_node`，读取已有任务的 snapshot 并返回，而不是错误地再次创建新 run。
   当前 `/chat` 已不再把所有 coding 请求都等同于“新建代码任务”；同时由于 `ChatServiceResult` 与 workflow result 增加了 `run_action` 边界，inspect 路径也不会误触发后台执行调度。

63. P7：chat 侧 run control 分支版。
   已继续把已有 run 的 `retry / rerun / cancel` 控制动作接入 LangGraph：当前当 prompt 中带有合法 `run_id` 且语义更接近控制请求时，`agent_workflow` 会直接走 `run_control_node`，调用现有 `run_interface` 中的 `retry_run / rerun_run / cancel_run`，随后统一读取目标 run 的 snapshot 并返回结构化说明。
   当前 `/chat` 已能够区分 `create / inspect / retry / rerun / cancel` 五类 run action；其中 `create / retry / rerun` 会继续触发后台执行调度，`inspect / cancel` 不会误触发新的后台 run。

64. P7：run inspection 阶段化反馈版。
   已继续把 `inspect` 分支从“统一回显 snapshot”调整为“按阶段反馈”：当前当目标 run 仍处于 `queued / running` 中间态时，LangGraph 会返回更贴合阶段的进展说明，并补上最近一次 attempt 摘要；当目标 run 已进入 `done / failed / cancelled` 终态时，则会优先走最终结果总结输出，而不是继续沿用中间态快照文案。
   当前 `P7` 已基本形成“创建 / 读取 / 控制 / 阶段化反馈 / 最终总结”的最小闭环；后续如无新的 run 协议需求，可以将主注意力切到 `P8` 和 `P9`。

65. P8：agent workflow trace + diagnostics preview 基础版。
   已开始为 `agent_workflow` 补充统一的 `workflow_trace`：当前 router、chat、coding、workspace tool、run create、run inspect、run control、unknown 和 roleplay 这些主节点都会在 state 中留下结构化 trace 记录，便于后续排查节点路由、`ui_status` 变化和 run action 决策。
   同时已新增 `POST /agent/diagnostics/preview`，用于在不产生真实副作用的前提下，预览某条输入会如何进入 LangGraph，包括 `intent`、`run_action`、workspace tool 规划结果、预期节点路径和 trace；这让 `P8` 至少具备了“先看清当前会怎么走”的最小调试入口，后续可继续补更细的节点日志和运行期诊断。

66. P8：agent runtime diagnostics 基础版。
   已继续新增 `POST /agent/diagnostics/run`，用于在受控前提下实际执行一遍安全的 Agent workflow 分支，并返回真实 `output / error / ui_status / workflow_trace`，帮助定位“preview 说会这样走，但真实执行时为什么这样收口”的问题。
   当前该入口默认只允许执行 `chat`、`unknown` 和 `coding + inspect` 这类无副作用或只读路径；对 `create / retry / rerun / cancel` 这类会创建新 run 或改变已有 run 状态的分支，仍明确拦截并返回 `blocked_reason`。这使 `P8` 已从“纯预览”推进到“安全运行期诊断”阶段，但更细的节点日志、错误上下文和统一运行期故障定位能力仍待继续补完。

67. P8：debug summary + error context 基础版。
   已继续为 `preview / run` 两类 diagnostics 统一补充 `debug_summary` 与 `error_context`：当前诊断结果不再只返回原始 `workflow_trace`，还会直接概括 `trace_count`、首尾节点、最终节点、最后事件、失败节点、失败事件，以及该次诊断是否属于主动拦截路径。
   对于真实执行失败或被安全拦截的 diagnostics，请求方现在可以直接读取 `error_context` 中的 `message / error_type / failure_node / failure_event / last_ui_status / suggested_next_step` 做快速定位；同时 `agent_support.append_workflow_trace()` 也已补上 debug 级节点日志，便于后续继续扩充运行期观测。

68. P8：node exception guard 基础版。
   已继续为 `agent_workflow` 主节点接入统一的 node exception guard：当前如果 `router / chat / coding / workspace_tool / run_tool / run_snapshot / run_control / unknown / roleplay` 中任一节点直接抛出异常，后端会优先把它收口为 `workflow_node_failed` 状态，并自动向 trace 写入 `node_exception` 事件，而不是让整条 LangGraph 链路因为异常而失去结构化诊断信息。
   当前 runtime diagnostics 在面对“正常失败返回”和“节点直接抛异常”两类故障时，已经都能给出统一的 `failure_node / failure_event / error_context`；`P8` 的主缺口因此已进一步缩小，后续更偏向补足更细粒度的运行期日志颗粒度与统一故障定位文案。

69. P8：workflow node metadata 统一版。
   已继续为 diagnostics 相关输出统一补充节点元信息：当前 `workflow_trace` 中的每个节点都会带上 `node_label` 与 `phase`，同时 `debug_summary` 与 `error_context` 也会同步暴露 `first_node_label / last_node_label / terminal_node_label / failure_node_label / last_phase / failure_phase`。
   当前无论是 `preview`、被安全拦截的 runtime diagnostics，还是实际执行后的 runtime diagnostics，都已经统一走同一套 trace 归一化逻辑；这让开发期排查时不再需要只盯着内部英文节点名，也更容易区分问题落在 `routing / chat / coding / tools / run_* / roleplay / diagnostics` 哪个阶段。

70. P8：failure descriptor 统一版。
   已继续把 diagnostics 的故障定位结果从“主要依赖 message 和原始 event”推进为“稳定错误码 + 故障域 + 中文摘要”模式：当前 `debug_summary` 已可直接返回 `failure_code / failure_domain`，`error_context` 也会同步返回 `summary / error_code / failure_domain`。
   当前被安全拦截的路径、LLM 失败返回、节点直接抛异常，以及后续 `workspace tool / run create / run snapshot / run control` 等常见失败类型，都已经可以通过统一的 descriptor 规则做快速分类；这让 `P8` 在“统一故障定位文案”这一侧进一步收口，后续剩余空缺主要转向更细粒度的节点级观测。

71. P8：workflow trace node log 版。
   已继续把 `workflow_trace` 从“结构化事件数组”推进为“可直接阅读的节点级日志流”：当前每个 trace item 除了 `node / phase / event / details` 外，还会统一补充 `event_label / status_level / message`，用于表达这一步发生了什么、严重级别如何、排查时应优先看哪一类现象。
   当前 preview、runtime diagnostics、blocked diagnostics 以及运行失败路径都已经共享这套 trace enrich 逻辑；因此 `P8` 在“更细粒度的节点级观测”这一侧也已基本闭合，后续主线可以逐步切到 `P9` 的结构性去冗余和 workflow 边界收敛。

72. P9：diagnostics support 边界下沉版。
   已开始把 `diagnostics.py` 中持续膨胀的 trace enrich、failure descriptor 和 failure trace 识别逻辑下沉到独立 `diagnostics_support.py`，让主 diagnostics 文件更接近“接口编排 + summary / error_context 组装”职责。
   当前这一步仍属于纯重构，不改变 preview/runtime diagnostics 的对外行为；但它为后续继续拆分 workflow 侧 support/helper、避免单文件继续失控，打下了第一块实际的 `P9` 基础。

73. P9：agent run support 边界下沉版。
   已继续把 `agent_graph.py` 中 run 相关的共用辅助逻辑下沉到独立 `agent_run_support.py`，包括目标 `run_id` 解析、run snapshot fallback 读取、terminal summary 归一化，以及 run control action 的统一调度与 fallback next-action 选择。
   当前 `agent_graph.py` 已进一步向“节点编排 + 状态收口”职责靠拢，而不是继续堆叠 run 侧细节分支；同时也补了对应回归测试，确保这一步仍属于纯重构而不是行为变更。

74. P9：agent text/state support 拆分版。
   已继续把 `agent_support.py` 中两块最稳定、最容易重复膨胀的内容拆出：一块是 `agent_text_support.py`，负责 chat/run 侧输出文案骨架；另一块是 `agent_state_support.py`，负责 `merge_agent_state`、`append_workflow_trace`、`normalize_optional_text`、初始 state 构造和 graph invoke 收口等通用状态辅助逻辑。
   当前 `agent_support.py` 仍保留原有导出面，避免影响现有节点与测试调用；但其内部职责已经进一步聚焦为“coding/workspace/run 状态 builder”，这让 `P9` 的 workflow support 收敛又向前推进了一步。

75. P9：agent graph wiring/guard support 拆分版。
   已继续把 `agent_graph.py` 中 graph 注册、conditional edge 配置和 node exception guard 样板下沉到独立 `agent_graph_support.py`，并通过常量化的 edge map 与 linear edge 列表来收口 wiring 结构。
   当前 `agent_graph.py` 已更接近“节点函数定义 + graph 组装入口”职责，而 graph support 侧也补上了直接测试，用于锁住 guard 收口行为和 wiring 配置边界，避免后续再把这些样板堆回主图文件。

76. P9：agent support facade 收口版。
   已继续把 `agent_support.py` 中最后残留的大块 selector/builder 实现拆到 `agent_constants.py`、`agent_routing_support.py` 与 `agent_builder_support.py`，让 `agent_support.py` 最终收敛为一个轻量兼容导出层。
   当前 workflow 侧的 constants、routing、state、text、builder、run、diagnostics、graph support 已经形成较清晰的横向模块分工；同时保留了原有 `agent_support` 的主要导入面，避免对既有节点与测试使用方式造成破坏。这意味着 `P9` 的“workflow 边界收敛与持续去冗余”目标已基本完成。

当前 P1 已接入：

1. `/chat` 普通聊天开始、成功和失败。
2. `/runs` 任务排队、开始、修复、完成、失败和取消。

当前 P3 已接入：

1. `/chat` coding intent。
2. LangGraph router。
3. Run Tool Node 创建 run。
4. FastAPI 后台任务执行 run。
5. 普通 `chat intent` 进入 Chat Node。
6. Roleplay Node 负责统一收口，但直接 `/chat` 返回的正文不会重复写入 `agent:chat`。
7. `/runs` 在成功、失败、取消时会补发最终总结消息到 `agent:chat`，用于桌宠聊天窗口展示任务结果。
8. `run_summary_graph` 已可读取终态 run record，并统一生成最终用户可见总结消息。
9. `repair_decision_graph` 已可在执行失败后输出失败分析和是否进入自动修复的决策结果。
10. `repair_decision_graph` 已进一步承接修复脚本生成，使 repair 流程从“判断”推进到“判断 + 生成”。
11. `repair_decision_graph` 已可输出 attempt 级 repair feedback，并在真正进入重试前写入 `agent:chat`。
12. `attempt_summary_graph` 已可在 repair 重试结束后输出 attempt 级结果总结，并补齐 retry outcome feedback。
13. summary / repair 相关子图的初始 state、结果包装和 roleplay 收口已进一步下沉到 support helper，P3 内部样板代码明显减少。
14. `repair_decision_graph` 已开始直接输出 retry guidance，使 repair 中间态文案不再只由 service 层拼接。
15. retry 中间态的消息计划已开始收敛到共享 `RetryGuidance` 结果模型，graph 和 service 的边界更稳定。
16. retry 中间态的文案策略已进一步收敛到共享 `retry_guidance.py`，减少了跨层重复维护同类中文提示的风险。
17. repair 阶段的 resolution 已进一步下沉到 lifecycle helper，`execute_run()` 主循环不再分散处理 retry guidance fallback、repair feedback 和重复取消检查。
18. `run_summary_graph` 和 `attempt_summary_graph` 的返回值已进一步收敛到共享 `WorkflowSummaryResult`，summary 子图之间的结果边界更一致。
19. `repair_decision_graph` 的返回值已进一步收敛到共享 `WorkflowRepairResult`，repair 的 decision / workflow 两种出口不再各自维护独立结果包装逻辑。
20. API 异常输出已开始统一到共享 `ApiErrorResponse`，并补上了依赖层抛错 helper 与 router 前缀/标签声明，后续继续整理文档和接口说明时不需要再逐个接口手动对齐错误结构。
21. 聊天上下文已开始支持长历史压缩：更早历史会被压缩为确定性摘要，最近消息则继续原样保留，`conversation_store` 不再只在超长时简单尾部截断。
22. 会话持久化文件已开始写入 `metadata`，并新增会话元信息读取接口，长会话压缩的状态现在可以直接从后端接口观察。
23. repair attempt 在成功、执行中取消、收到取消请求后的终态收口已进一步下沉到 lifecycle helper，`execute_run()` 主循环不再分散维护这三类 retry outcome 分支。
24. `agent_graph.run_agent()` 已进一步收敛到共享 `WorkflowAgentResult`，主 Agent 链路不再对外暴露松散字典结果，chat service 消费 LangGraph 结果时的边界更稳定。
25. `repair_decision_graph -> lifecycle` 之间的 repair workflow 输入已进一步统一归一化，service 层不再分散兼容 `reason / feedback_text / retry_node_name` 等多种对象形态。
26. `run_summary_graph`、`attempt_summary_graph`、`repair_decision_graph` 已开始共享 prompt 拼接、文本结果写回和 summary invoke helper，graph 层样板代码进一步减少。
27. `repair_decision_graph` 已进一步把 workflow mode、feedback message、next-step route 和 invoke 收口到独立 `repair_support.py`，主 graph 文件的 decision / feedback 骨架继续变薄。
28. `agent_graph` 已进一步把 route、initial state、fallback output 和 invoke 收口到独立 `agent_support.py`，主图文件开始向“节点定义 + 编排”职责收敛。
29. `agent_graph` 各节点的状态写回和 roleplay 收口已进一步共享到 `agent_support.py`，主图节点内部的样板状态拼装明显减少。
30. `agent`、`summary`、`repair` 三类工作流的 invoke 行为已进一步统一：graph 正常完成时返回结构化 result，graph 异常时也会收口成失败 result；run 生命周期侧则会继续对失败的 summary result 执行确定性 fallback，避免消息静默丢失。
31. `repair_decision_graph` 各节点的状态写回已进一步共享到 `repair_support.py`，repair 子图文件继续向“节点定义 + 编排”职责收敛。
32. 运行生命周期中两条 summary fallback 链路已进一步共享到统一 helper，summary graph 返回失败 result 或直接抛异常时都会稳定回退到确定性聊天消息。
33. `run_summary_graph` 和 `attempt_summary_graph` 的 summary node / roleplay node 构造已进一步共享到 `summary_support.py`，summary 子图文件继续向“配置 + 编排”职责收敛。
34. workflow 里的主要 `node_name` 常量和 run 终态节点选择逻辑已进一步共享到 `workflow_nodes.py`，消息策略相关的字符串漂移风险进一步下降。
35. workflow 的主要用户可见聊天文案骨架已进一步共享到 `build_run_chat_text()`，repair / retry / completion 三类消息开始围绕同一套文本装配策略协作。
36. summary / repair 两类子图的“LLM 文本解析 -> state 写回”骨架已进一步共享到 `build_text_resolution_node()`，`repair_decision_graph` 的 QA 分析节点不再单独维护一套近似的 resolution 逻辑。
37. retry 中间态里的终态节点选择、repair result fallback 和 next_action 文案映射已进一步共享到 `retry_guidance.py`，`lifecycle.py` 不再自己维护一套近似的 retry policy 分支。
38. workflow 内部的 state 发消息、message 对象发消息和 fallback 文本发消息入口已进一步共享到 `emit_roleplay_message()` / `emit_roleplay_state()`，`agent_support.py`、`summary_support.py` 和 `lifecycle.py` 不再各自维护近似的 chat 发送胶水。
39. `WorkflowAgentResult -> ChatServiceResult` 的归一化和 chat service 结果更新已进一步共享到 `ChatServiceResult.from_agent_result()` / `with_updates()`，`chat_action/agent.py` 与 `chat_interface.py` 不再各自维护近似的结果拆解和原地字段改写。
40. `WorkflowRepairResult` 的 analysis / feedback / repaired_result 消费入口已进一步共享到 `analysis_log_payload()` / `feedback_payload()` / `repaired_result_or()`，`lifecycle.advance_repair_phase()` 不再自己拼装同类字段读取逻辑。
41. `WorkflowSummaryResult` 的归一化与 fallback 发消息入口已进一步共享到 `from_value()` / `emit_summary_workflow_with_fallback()`，`emit_final_run_chat_message()` 与 `emit_retry_outcome_message()` 不再各自维护近似的 summary result 消费控制流。
42. `WorkflowAgentResult` 的 object/mapping 归一化入口已进一步共享到 `from_value()`，`ChatServiceResult.from_agent_result()` 不再自己直接对任意对象做 `intent / output / run_id` 拆解。

当前 P2 已进一步接入：

1. 会话历史写入 `workspace/conversations/`。
2. 服务重启后可继续读取同一 `session_id` 的最近历史。
3. `clear_session` 会同时清理内存和磁盘侧缓存。
4. 会话持久化文件支持 TTL 过期清理。
5. 会话持久化文件支持最大数量控制。
6. 启动阶段和运行阶段都会按配置执行会话清理。
7. 长会话压缩摘要会写入会话 `metadata`，作为可持久化复用的 summary cache。
8. 如果上下文压缩策略配置未变化，服务重启后会优先复用摘要缓存；如果配置变化，则会自动重建缓存并刷新元信息。
9. `GET /chat/sessions/{session_id}` 已可直接观察 `has_summary_cache` 和 `context_strategy_version`。
10. `GET /chat/sessions` 已可分页查看最近会话列表，便于调试会话持久化、压缩和摘要缓存命中情况。

当前 P4 已接入：

1. `backend/app/api/health_routes.py`
2. `backend/app/api/llm_routes.py`
3. `backend/app/api/chat_routes.py`
4. `backend/app/api/run_routes.py`
5. `backend/app/api/message_routes.py`
6. `main.py` 已改为 app factory + router 挂载模式。
7. `RunActionError` 已收敛为 app 级统一异常处理。
8. run 相关读取逻辑已下沉到 `run_dependencies.py`。
9. 未预期异常的 `500` 响应已统一到共享 `ApiErrorResponse` 结构。
10. 接口层的共享查询参数、run 后台调度和部分响应组装已下沉到 route helper。

下一步建议进入：

1. 继续细化 P8：在已有 `workflow_trace`、`diagnostics preview`、`runtime diagnostics`、`debug summary`、`error_context` 和 `node exception guard` 基础上，继续补更细的节点级日志和更统一的故障定位文案。
2. 进入 P9：继续收口 `agent_workflow` 内部的 node/state 边界，避免 `agent_graph.py` 和 support/helper 模块再次膨胀。
3. 在 P8/P9 推进过程中，保持对 `/chat -> LangGraph -> /runs -> /messages` 主链路的回归测试，避免诊断层和去冗余改动带来行为漂移。

## 10. 队长反馈后的架构修正

队长指出：`LongCat` 是大模型本身；项目真正需要的是 `LangGraph`，因为 LangGraph 才能组织 Agent 的状态流和工具调用。

本计划据此修正为：

1. `LongCat` 只作为 LLM provider，不再把“接入 LongCat”等同于“完成 Agent”。
2. `LangGraph` 作为下一阶段核心主线，用来承载 Intent Router、Chat Node、Coding Node、Run Tool Node、Roleplay Node。
3. 现有 `/runs`、safe tools、message protocol、character events、conversation store 都保留，作为 LangGraph 节点可以调用的能力。
4. P3 不再只是“coding intent 直接接 `/runs`”，而是“coding intent 进入 LangGraph，再由 LangGraph 调用 `/runs` 和工具层”。
5. 多 Agent 公司化架构后置，先实现一个最小可用 LangGraph Agent Brain。

修正后的最小闭环：

```text
用户输入
  -> /chat
  -> LangGraph router
  -> chat_node 或 coding_node
  -> run_tool_node 或 llm_node
  -> roleplay_node
  -> /messages + ChatResponse
```
